name: publish

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: snorkysnark/ralsei-py/.github/setup-action
      - name: Print version
        run: echo $LATEST_TAG
